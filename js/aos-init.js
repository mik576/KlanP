document.addEventListener("DOMContentLoaded", function () {
    AOS.init({
        duration: 1000,  // Thời gian hiệu ứng (ms)
        once: true       // Hiệu ứng chỉ chạy 1 lần khi cuộn
    });
});